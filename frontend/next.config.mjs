/** @type {import('next').NextConfig} */
const nextConfig = {
  output: "standalone",
  reactStrictMode: true,
  // For CF Pages static export in future: output: "export"
};

export default nextConfig;